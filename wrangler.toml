name = "endpr"
main = "src/index.ts" # Placeholder, will be adjusted by Next.js adapter later
compatibility_date = "2024-04-05"

[[d1_databases]]
binding = "DB" # The binding name used in the Worker
database_name = "endpr_db"
database_id = "DO_NOT_USE_IN_LOCAL_MODE" # Placeholder
preview_database_id = "DO_NOT_USE_IN_LOCAL_MODE"

[[r2_buckets]]
binding = "R2_ASSETS" # The binding name used in the Worker
bucket_name = "endpr-assets"
